# Database (local or remote)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=drogueria_db

# Optional single URL (for Supabase or remote DB)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# SSL flag for remote DBs
DB_SSL=false